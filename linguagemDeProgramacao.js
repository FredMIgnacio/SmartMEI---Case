//Utilizar uma linguagem de programação de sua preferência

//1.
const greaterValue = function(a,b) {
    if (a > b) {
        return a
    }
    return b
};

//2.
const ascendingOrder = function(a,b) {
    if (a > b) {
        return [b,a]
    }
    return [a,b]
};